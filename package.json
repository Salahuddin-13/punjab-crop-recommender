{
  "name": "punjab-crop-recommender-app",
  "version": "0.1.0",
  "private": true,
  "proxy": "http://localhost:5000",
  "dependencies": {
    "@testing-library/dom": "^10.4.1",
    "@testing-library/jest-dom": "^6.8.0",
    "@testing-library/react": "^16.3.0",
    "@testing-library/user-event": "^13.5.0",
    "axios": "^1.12.2",
    "framer-motion": "^12.23.12",
    "lucide-react": "^0.542.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-helmet": "^6.1.0",
    "react-helmet-async": "^2.0.5",
    "react-icons": "^5.5.0",
    "react-router-dom": "^7.8.2",
    "react-scripts": "5.0.1",
    "recharts": "^3.1.2",
    "serve": "^14.2.0",
    "web-vitals": "^2.1.4",
    "workbox-build": "^5.1.4",
    "workbox-webpack-plugin": "^5.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "serve": "serve -s build",
    "server": "cd backend && npm run dev",
    "dev": "concurrently \"npm run server\" \"npm start\"",
    "backend-setup": "cd backend && npm install",
    "build-full": "npm run build && cd backend && npm start",
    "deploy": "npm run build && cd backend && npm start",
    "install-all": "npm install && cd backend && npm install",
    "test-backend": "curl http://localhost:5000/api/health",
    "clean": "rm -rf node_modules backend/node_modules build",
    "reset": "npm run clean && npm run install-all"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "concurrently": "^8.2.2",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.17"
  }
}
